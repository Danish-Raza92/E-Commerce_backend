# =============================================
# DANII.STORE BACKEND — Environment Variables
# =============================================
# Railway pe yeh sab Settings > Variables mein add karo
# LOCAL ke liye: cp .env.example .env

# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://draza9246_db_user:<db_password>@cluster0.znrfa7f.mongodb.net/danii-store?retryWrites=true&w=majority&appName=Cluster0

# JWT Secret (generate: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=your_super_secret_jwt_key_change_this

# Server
PORT=8080
NODE_ENV=production

# Vercel frontend URL (CORS ke liye)
FRONTEND_URL=https://e-commerce-frontend-theta-orpin.vercel.app
